public class MaxSubArrMinProduct {
    public long maxSumMinProduct(int[] nums) {
        long i = 0, j = 1;

        while (i < nums.length || j < nums.length) {
            
        }

        return 0;
    }

    public static long calcMinProduct(int[] prefixSum, long i, long j) {
        prefixSum[]
    }
}